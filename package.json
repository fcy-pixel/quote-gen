{
  "name": "quote-gen",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev public --compatibility-date=2024-09-23",
    "deploy": "npx wrangler pages deploy public --project-name quote-gen"
  },
  "devDependencies": {
    "wrangler": "^3"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  }
}
